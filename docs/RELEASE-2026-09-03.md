# Verificação de publicação — 03/09/2026

## Escopo e validação local

Versão Luz que Reconcilia, agenda confirmada, galeria Eventos da Reconciliação,
blog de Tessalonicenses, navegação Livros e catálogo de recomendações.

- Incorporados por fast-forward os 151 commits remotos de notícias, até `9766aa3`.
  Nenhuma edição remota foi descartada. RSS e JSON Feed regenerados desse acervo.
- 20 testes Node e 6 testes Python passaram; typecheck e build passaram.
- ESLint: zero erros e 7 avisos preexistentes de Fast Refresh nos componentes UI.
- `npm audit`: zero vulnerabilidades reportadas nesta verificação.
- Workflows Security Scan e Automated Backup alinhados com Node 24, compatível
  com o Vite atual. Os demais comportamentos desses workflows foram preservados.
- Perfis de navegador, originais pesados e capturas de revisão não são publicação.

## Supabase: verificação somente de leitura

O bundle público da versão anterior referencia dois projetos. As credenciais
usadas no teste eram públicas, com papel `anon`, e não foram salvas nem registradas.

- `atvchidakkzgtlszwmvn`: GET limitado a uma linha de `news_articles` retornou
  HTTP 200. O registro mais recentemente criado e visível nessa consulta data de
  21/08/2026. Isso confirma disponibilidade de leitura, não integridade completa,
  autorização de escrita, tamanho total, RLS ou funcionamento da ingestão.
- `kkhqybinqwrndcoeotnu`: o hostname não resolveu no DNS desta máquina.

O [News Scraper de 02/09/2026](https://github.com/carloshg-dev/mbda-faith-web-revival/actions/runs/33697472288)
terminou com status `success`, mas seus logs registram falha ao gravar no Supabase:
`23505`, duplicidade em `news_articles_url_key`. As leituras retornaram HTTP 200 e
os arquivos JSON foram atualizados. Portanto, sucesso do workflow antigo não
comprova sucesso de gravação no banco.

Não foram feitas escritas, exclusões, migrations, alterações de políticas ou
mudanças nos secrets do Supabase/GitHub durante esta verificação.

## Fluxo efetivo desta versão

`GitHub Actions → coleta limitada → JSON no Git → build/deploy → leitura estática`.

O navegador lê `/data/christian_news.json`, sem consultar o Supabase por visitante.
O News Scraper permanece habilitado, com cron às 07h e 19h de Brasília, sujeito a
atrasos do agendador. Sua configuração nova usa `NEWS_WRITE_SUPABASE=false` e
`NEWS_DISCORD_NOTIFY=false`. Os secrets antigos de URL e chave anon existem,
mas não são fornecidos a esse novo workflow. A réplica opcional ao banco exige
configuração explícita e credencial de servidor; não foi reativada nesta entrega.
O workflow novo não foi disparado manualmente, para evitar gravações como teste.

## Limite externo de publicação

Antes do push, o status Vercel do commit remoto `9766aa3` já indicava
`failure: Account is blocked.`. O domínio `www.igrejadarecon.com.br` não resolveu
no DNS desta máquina; o endereço público que respondeu foi
`https://igrejadareconciliacao.vercel.app/`, ainda com a versão anterior.

Commit/push no GitHub não são confirmação de deploy. Enquanto o bloqueio da conta
Vercel persistir, não se pode afirmar que o novo site ou as novas edições de
notícias chegaram ao endereço público. A regularização da conta requer seu titular.
